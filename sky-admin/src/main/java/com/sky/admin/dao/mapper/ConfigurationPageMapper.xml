<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sky.admin.dao.ConfigurationPageDao">

    <sql id="showField" > coding,name,table_en,pk_field_en </sql>
    <sql id="tableEn" > s_configuration_page </sql>

    <!--映射实体类:配置页面-->
    <resultMap id="ConfigurationPageResultMap" type="com.sky.admin.po.ConfigurationPage">
        <id property="coding" column="coding"/>
        <result property="name" column="name"/>
        <result property="tableEn" column="table_en"/>
        <result property="pkFieldEn" column="pk_field_en"/>
    </resultMap>

    <!--查询全部数据-->
    <select id="findAll" parameterType="java.util.List"  resultMap="ConfigurationPageResultMap">
        SELECT <include refid="showField" /> FROM <include refid="tableEn" />
    </select>
    <!--查询-->
    <select id="findById" parameterType="java.lang.String" resultMap="ConfigurationPageResultMap">
        SELECT <include refid="showField" /> FROM <include refid="tableEn" /> WHERE coding=#{coding}
    </select>
    <!--按条件查询-->
    <select id="findByCondition" parameterType="com.sky.admin.po.ConfigurationPage" resultMap="ConfigurationPageResultMap">
        SELECT <include refid="showField" /> FROM <include refid="tableEn" />
        <trim prefix="WHERE" prefixOverrides="AND | OR">
            <if test="coding != null">
                AND coding LIKE CONCAT(CONCAT('%', #{coding}),'%')
            </if>
            <if test="name != null">
                AND name LIKE CONCAT(CONCAT('%', #{name}),'%')
            </if>
            <if test="tableEn != null">
                AND table_en LIKE CONCAT(CONCAT('%', #{tableEn}),'%')
            </if>
            <if test="pkFieldEn != null">
                AND pk_field_en LIKE CONCAT(CONCAT('%', #{pkFieldEn}),'%')
            </if>
        </trim>
    </select>
    <!--增加-->
    <insert id="doAdd" parameterType="com.sky.admin.po.ConfigurationPage">
        INSERT INTO <include refid="tableEn" />
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="coding != null">coding,</if>
            <if test="name != null">name,</if>
            <if test="tableEn != null">table_en,</if>
            <if test="pkFieldEn != null">pk_field_en,</if>
        </trim>
        VALUES
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="coding != null">#{coding},</if>
            <if test="name != null">#{name},</if>
            <if test="tableEn != null">#{tableEn},</if>
            <if test="pkFieldEn != null">#{pkFieldEn},</if>
        </trim>
    </insert>
    <!--更新-->
    <update id="doUpdate" parameterType="com.sky.admin.po.ConfigurationPage">
        UPDATE <include refid="tableEn" />
        <trim prefix="SET" suffixOverrides=",">
            <if test="name != null">name=#{name},</if>
            <if test="tableEn != null">table_en=#{tableEn},</if>
            <if test="pkFieldEn != null">pk_field_en=#{pkFieldEn},</if>
        </trim>
        WHERE coding=#{coding}
    </update>
    <!--批量删除-->
    <delete id="doDeleteBatch" parameterType="java.util.List">
        DELETE FROM <include refid="tableEn" />
        <where>
            coding IN
            <foreach collection="list" item="ele" open="(" separator="," close=")">
                '${ele}'
            </foreach>
        </where>
    </delete>
    <!--删除-->
    <delete id="doDelete" parameterType="java.lang.String">
        DELETE FROM <include refid="tableEn" />  WHERE coding = #{coding}
    </delete>


</mapper>